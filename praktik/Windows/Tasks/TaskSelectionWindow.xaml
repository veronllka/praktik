<Window x:Class="praktik.TaskSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Выбор задачи"
        Height="520"
        Width="840"
        WindowStartupLocation="CenterScreen"
        Style="{StaticResource GlassWindowStyle}">
    <Grid Style="{StaticResource GlassWindowRootStyle}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource GlassToolbarStyle}">
            <TextBlock Text="Выберите задачу для создания заявки на материалы"
                       Style="{StaticResource SubHeaderTextStyle}"
                       Margin="0"/>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource GlassDataGridHostStyle}">
            <DataGrid x:Name="dgTasks"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Название" Binding="{Binding Title}" Width="*"/>
                    <DataGridTextColumn Header="Объект" Binding="{Binding Site.SiteName}" Width="150"/>
                    <DataGridTextColumn Header="Бригада" Binding="{Binding Crew.CrewName}" Width="150"/>
                    <DataGridTextColumn Header="Статус" Binding="{Binding TaskStatus.TaskStatusName}" Width="120"/>
                    <DataGridTextColumn Header="Начало" Binding="{Binding StartDate, StringFormat=dd.MM.yyyy}" Width="100"/>
                    <DataGridTextColumn Header="Окончание" Binding="{Binding EndDate, StringFormat=dd.MM.yyyy}" Width="110"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource GlassFooterBarStyle}">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="btnSelect"
                        Content="Выбрать"
                        Click="BtnSelect_Click"
                        Style="{StaticResource AppRaisedButton}"
                        Width="130"
                        Margin="0,0,10,0"/>
                <Button x:Name="btnCancel"
                        Content="Отмена"
                        Click="BtnCancel_Click"
                        Style="{StaticResource AppOutlinedButton}"
                        Width="130"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
