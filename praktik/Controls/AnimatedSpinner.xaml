<UserControl x:Class="praktik.Controls.AnimatedSpinner"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="24" Height="24"
             IsVisibleChanged="OnVisibleChanged">
    <Canvas Width="24" Height="24" HorizontalAlignment="Center" VerticalAlignment="Center">
        <!-- Track ring -->
        <Ellipse Width="20" Height="20"
                 Canvas.Left="2" Canvas.Top="2"
                 Stroke="#33FFFFFF" StrokeThickness="2.5"/>
        <!-- Animated arc -->
        <Path x:Name="SpinnerArc"
              Stroke="{Binding AccentBrush, RelativeSource={RelativeSource AncestorType=UserControl}, FallbackValue=#0A84FF}"
              StrokeThickness="2.5"
              StrokeLineCap="Round"
              Data="M 12,2 A 10,10 0 0 1 22,12">
            <Path.RenderTransform>
                <RotateTransform x:Name="SpinnerRotation" CenterX="12" CenterY="12"/>
            </Path.RenderTransform>
        </Path>
    </Canvas>

    <UserControl.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard x:Name="SpinStoryboard">
                <Storyboard RepeatBehavior="Forever">
                    <DoubleAnimation
                        Storyboard.TargetName="SpinnerRotation"
                        Storyboard.TargetProperty="Angle"
                        From="0" To="360"
                        Duration="0:0:0.9">
                        <DoubleAnimation.EasingFunction>
                            <SineEase EasingMode="EaseInOut"/>
                        </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </UserControl.Triggers>
</UserControl>
