<Window x:Class="praktik.QRCodeScannerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Сканирование QR-кода" Height="300" Width="500" WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Введите или вставьте QR-код задачи" 
                   FontSize="16" FontWeight="SemiBold" 
                   Margin="0,0,0,16" Grid.Row="0"/>

        <TextBlock Text="Формат: число (ID задачи) или task:123" 
                   FontSize="12" Foreground="#666666" 
                   Margin="0,0,0,12" Grid.Row="1"/>

        <TextBox x:Name="txtQRCode" 
                 Grid.Row="2"
                 FontSize="14" 
                 Padding="12,10"
                 Margin="0,0,0,16"
                 KeyDown="txtQRCode_KeyDown"
                 TextChanged="txtQRCode_TextChanged"/>

        <TextBlock x:Name="txtError" 
                   Grid.Row="3"
                   Foreground="#E53E3E" 
                   TextWrapping="Wrap"
                   FontSize="12"
                   Visibility="Collapsed"
                   Margin="0,0,0,16"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Открыть задачу" 
                    x:Name="btnOpenTask" 
                    Click="btnOpenTask_Click" 
                    Width="120" 
                    Margin="0,0,10,0"
                    IsEnabled="False"/>
            <Button Content="Отмена" 
                    Click="btnCancel_Click" 
                    Width="100"/>
        </StackPanel>
    </Grid>
</Window>

