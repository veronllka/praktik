<Window x:Class="praktik.QuickNoteWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Быстрая заметка" Height="150" Width="500" WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Короткая пометка…" FontSize="12" Foreground="#666666" Margin="0,0,0,8"/>

        <TextBox x:Name="txtNote" 
                Grid.Row="1" 
                Height="35" 
                FontSize="14"
                TextChanged="txtNote_TextChanged"
                KeyDown="txtNote_KeyDown"
                VerticalContentAlignment="Center"
                Margin="0,0,0,15"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="btnSave" 
                   Content="Сохранить" 
                   Click="btnSave_Click" 
                   Width="100" 
                   Height="30"
                   Margin="0,0,10,0"
                   IsEnabled="False"/>
            <Button x:Name="btnCancel" 
                   Content="Отмена" 
                   Click="btnCancel_Click" 
                   Width="80" 
                   Height="30"/>
        </StackPanel>
    </Grid>
</Window>


